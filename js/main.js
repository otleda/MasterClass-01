// GET KEY ALL
const keys = document.querySelectorAll('.key')

// PLAY NOTES

// KEYBOARDS TYPE
window.addEventListener('keydown', event => {

    // keycodejs/main.js
    // typed key pressed
    // if key exist
    // play audio
    console.log(event)
})

// click with mouse